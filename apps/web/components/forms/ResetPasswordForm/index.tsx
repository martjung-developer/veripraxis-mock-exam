'use client'

import styles from './reset-password-form.module.css'

export default function ResetPasswordForm() {
  return (
    <form className={styles.form}>
      <p>ResetPasswordForm fields go here.</p>
    </form>
  )
}
