'use client'

import styles from './create-exam-form.module.css'

export default function CreateExamForm() {
  return (
    <form className={styles.form}>
      <p>CreateExamForm fields go here.</p>
    </form>
  )
}
